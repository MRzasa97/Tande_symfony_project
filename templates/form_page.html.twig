{% extends 'base.html.twig' %}

{% block body %}
    <h1>Formularz</h1>

    {% for flashMessage in app.flashes('success') %}
        <div class="alert alert-success">
            {{ flashMessage }}
        </div>
    {% endfor %}

    {{ form_start(form) }}
    {{ form_row(form.imie) }}
    {{ form_row(form.nazwisko) }}
    {{ form_row(form.zalacznik) }}

    <button type="submit" class="btn btn-primary">Wyślij</button>
    {{ form_end(form) }}
{% endblock %}